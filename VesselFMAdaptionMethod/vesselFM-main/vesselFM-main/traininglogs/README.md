# Training Logs

- `vessel_ct_vessel_curve.csv` shows the training run for performing the Stage 2: S1, S2, & S3 training for 3-class output.

- `av_ct_training_curve.csv` shows the training run for performing the Stage 1 fine tuning of vesselFM base checkpoint.
